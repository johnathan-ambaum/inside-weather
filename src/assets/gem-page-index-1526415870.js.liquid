{% comment %}
	GEMPAGE BUILDER (https://apps.shopify.com/gempage)

	You SHOULD NOT modify source code in this page because
	It is automatically generated from GEMPAGE BUILDER
	Try to edit page with the live editor.
{% endcomment %}

        jQuery(function() {
            var $module = jQuery('#m-1526418629250');

            $module.css({
                'height': 'auto'
            });
            jQuery(window).resize(function(){
                $module.css('height', 'auto');
            });
            $module.children('.module').removeClass('full-height');


            var image = $module.children('.module').data('image') || '';
            var speed = parseFloat($module.children('.module').data('speed')) || 0.2;
            if($module.data('gfparallax') != undefined) {
                $module.removeData('gfparallax');
            }
            $module.gfParallax({
                'image': image,
                'speed': speed
            });
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1529367452416');

            $module.css({
                'height': 'auto'
            });
            jQuery(window).resize(function(){
                $module.css('height', 'auto');
            });
            $module.children('.module').removeClass('full-height');


            var image = $module.children('.module').data('image') || '';
            var speed = parseFloat($module.children('.module').data('speed')) || 0.2;
            if($module.data('gfparallax') != undefined) {
                $module.removeData('gfparallax');
            }
            $module.gfParallax({
                'image': image,
                'speed': speed
            });
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1529525347851');

            $module.css({
                'height': 'auto'
            });
            jQuery(window).resize(function(){
                $module.css('height', 'auto');
            });
            $module.children('.module').removeClass('full-height');


            var image = $module.children('.module').data('image') || '';
            var speed = parseFloat($module.children('.module').data('speed')) || 0.2;
            if($module.data('gfparallax') != undefined) {
                $module.removeData('gfparallax');
            }
            $module.gfParallax({
                'image': image,
                'speed': speed
            });
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1528406541960').children('.module');   
            var navspeed = $module.data('navspeed'),
                margin = parseInt($module.data('margin')),
                autoplaytimeout = $module.data('autoplaytimeout'),
                autoplayhoverpause = $module.data('autoplayhoverpause'),
                navlg = $module.data('navlg'),
                navmd = $module.data('navmd'),
                navsm = $module.data('navsm'),
                navxs = $module.data('navxs'),
                collg = $module.data('collg'),
                colmd = $module.data('colmd'),
                colsm = $module.data('colsm'),
                colxs = $module.data('colxs'),
                dotslg = $module.data('dotslg'),
                dotsmd = $module.data('dotsmd'),
                dotssm = $module.data('dotssm'),
                dotsxs = $module.data('dotsxs');

            var mode = jQuery('.gryffeditor').hasClass('editing') ? 'dev' : 'production';
            if(mode == 'production') {
                var autoplay = $module.data('autoplay'), 
                    loop = $module.data('loop');
            } else {
                var autoplay = 0, 
                    loop = 0;
            }
        
            $module.owlCarousel({
                margin: margin,
                mouseDrag: false,
                autoplayHoverPause: autoplayhoverpause,
                autoplay: autoplay,
                autoplayTimeout: autoplaytimeout,
                loop: loop,
                navSpeed: navspeed,
                autoWidth: !1,
                responsiveClass:true,
                responsive:{
                    0:{
                        items:colxs,
                        nav: navxs,
                        dots:dotsxs
                    },
                    768:{
                        items:colsm,
                        nav: navsm,
                        dots:dotssm
                    },
                    992:{
                        items:colmd,
                        nav: navmd,
                        dots:dotsmd
                    },
                    1200:{
                        items:collg,
                        nav: navlg,
                        dots:dotslg
                    }
                }
            }); 
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526424685472').children('.module');   
            var nav = $module.data('nav'), 
                navspeed = $module.data('navspeed'),
                margin = parseInt($module.data('margin')),
                autoplaytimeout = $module.data('autoplaytimeout'),
                autoplayhoverpause = $module.data('autoplayhoverpause'),
                collg = $module.data('collg'),
                colmd = $module.data('colmd'),
                colsm = $module.data('colsm'),
                colxs = $module.data('colxs'),
                dotslg = $module.data('dotslg'),
                dotsmd = $module.data('dotsmd'),
                dotssm = $module.data('dotssm'),
                dotsxs = $module.data('dotsxs');

            var mode = jQuery('.gryffeditor').hasClass('editing') ? 'dev' : 'production';
            if(mode == 'production') {
                var autoplay = $module.data('autoplay'), 
                    loop = $module.data('loop');
            } else {
                var autoplay = 0, 
                    loop = 0;
            }
        
            $module.owlCarousel({
                margin: margin,
                mouseDrag: false,
                nav: nav,
                autoplayHoverPause: autoplayhoverpause,
                autoplay: autoplay,
                autoplayTimeout: autoplaytimeout,
                loop: loop,
                navSpeed: navspeed,
                autoWidth: !1,
                responsiveClass:true,
                responsive:{
                    0:{
                        items:colxs,
                        dots:dotsxs
                    },
                    768:{
                        items:colsm,
                        dots:dotssm
                    },
                    992:{
                        items:colmd,
                        dots:dotsmd
                    },
                    1200:{
                        items:collg,
                        dots:dotslg
                    }
                }
            }); 
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1529526311242').children('.module');   
            var nav = $module.data('nav'), 
                navspeed = $module.data('navspeed'),
                margin = parseInt($module.data('margin')),
                autoplaytimeout = $module.data('autoplaytimeout'),
                autoplayhoverpause = $module.data('autoplayhoverpause'),
                collg = $module.data('collg'),
                colmd = $module.data('colmd'),
                colsm = $module.data('colsm'),
                colxs = $module.data('colxs'),
                dotslg = $module.data('dotslg'),
                dotsmd = $module.data('dotsmd'),
                dotssm = $module.data('dotssm'),
                dotsxs = $module.data('dotsxs');

            var mode = jQuery('.gryffeditor').hasClass('editing') ? 'dev' : 'production';
            if(mode == 'production') {
                var autoplay = $module.data('autoplay'), 
                    loop = $module.data('loop');
            } else {
                var autoplay = 0, 
                    loop = 0;
            }
        
            $module.owlCarousel({
                margin: margin,
                mouseDrag: false,
                nav: nav,
                autoplayHoverPause: autoplayhoverpause,
                autoplay: autoplay,
                autoplayTimeout: autoplaytimeout,
                loop: loop,
                navSpeed: navspeed,
                autoWidth: !1,
                responsiveClass:true,
                responsive:{
                    0:{
                        items:colxs,
                        dots:dotsxs
                    },
                    768:{
                        items:colsm,
                        dots:dotssm
                    },
                    992:{
                        items:colmd,
                        dots:dotsmd
                    },
                    1200:{
                        items:collg,
                        dots:dotslg
                    }
                }
            }); 
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526593108713');

            $module.css({
                'height': 'auto'
            });
            jQuery(window).resize(function(){
                $module.css('height', 'auto');
            });
            $module.children('.module').removeClass('full-height');


            var image = $module.children('.module').data('image') || '';
            var speed = parseFloat($module.children('.module').data('speed')) || 0.2;
            if($module.data('gfparallax') != undefined) {
                $module.removeData('gfparallax');
            }
            $module.gfParallax({
                'image': image,
                'speed': speed
            });
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1529523810707');

            $module.css({
                'height': 'auto'
            });
            jQuery(window).resize(function(){
                $module.css('height', 'auto');
            });
            $module.children('.module').removeClass('full-height');


            var image = $module.children('.module').data('image') || '';
            var speed = parseFloat($module.children('.module').data('speed')) || 0.2;
            if($module.data('gfparallax') != undefined) {
                $module.removeData('gfparallax');
            }
            $module.gfParallax({
                'image': image,
                'speed': speed
            });
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526595272126');

            $module.css({
                'height': 'auto'
            });
            jQuery(window).resize(function(){
                $module.css('height', 'auto');
            });
            $module.children('.module').removeClass('full-height');


            var image = $module.children('.module').data('image') || '';
            var speed = parseFloat($module.children('.module').data('speed')) || 0.2;
            if($module.data('gfparallax') != undefined) {
                $module.removeData('gfparallax');
            }
            $module.gfParallax({
                'image': image,
                'speed': speed
            });
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932').children('.module');
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child1').children('.module');
            $module.gfV3Product();
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child1-0').children('.module');
            var effect = $module.attr('data-effect');
            $module.gfV3ProductImage({
                'effect': effect
            })
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child2').children('.module');
            $module.gfV3Product();
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child2-0').children('.module');
            var effect = $module.attr('data-effect');
            $module.gfV3ProductImage({
                'effect': effect
            })
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child3').children('.module');
            $module.gfV3Product();
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child3-0').children('.module');
            var effect = $module.attr('data-effect');
            $module.gfV3ProductImage({
                'effect': effect
            })
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child4').children('.module');
            $module.gfV3Product();
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child4-0').children('.module');
            var effect = $module.attr('data-effect');
            $module.gfV3ProductImage({
                'effect': effect
            })
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child5').children('.module');
            $module.gfV3Product();
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child5-0').children('.module');
            var effect = $module.attr('data-effect');
            $module.gfV3ProductImage({
                'effect': effect
            })
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child6').children('.module');
            $module.gfV3Product();
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child6-0').children('.module');
            var effect = $module.attr('data-effect');
            $module.gfV3ProductImage({
                'effect': effect
            })
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child7').children('.module');
            $module.gfV3Product();
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child7-0').children('.module');
            var effect = $module.attr('data-effect');
            $module.gfV3ProductImage({
                'effect': effect
            })
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child8').children('.module');
            $module.gfV3Product();
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508876932-child8-0').children('.module');
            var effect = $module.attr('data-effect');
            $module.gfV3ProductImage({
                'effect': effect
            })
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1526508988085').children('.module');

        }); 
    
var gemFlag=!1;for(var i=0;i<pageLibs.length;i++){if(pageLibs[i].indexOf('gfv3product.js')!==-1){if(jQuery.gfV3Product==undefined){gemFlag=!0;break}}if(pageLibs[i].indexOf('gfv3restabs.js')!==-1){if(jQuery.gfV3ResTabs==undefined){gemFlag=!0;break}}if(pageLibs[i].indexOf('gfaccordion.js')!==-1){if(jQuery.gfAccordion==undefined){gemFlag=!0;break}}}if(gemFlag){var count=0;for(var i=0;i<pageLibs.length-1;i++){jQuery.getScript(pageLibs[i],function(){count++;if(count==pageLibs.length-1){jQuery.getScript(pageLibs[pageLibs.length-1],function(){})}})}}