.ProductDetailGrid {
  display: grid;
  grid-gap: 3rem;
  grid-template-columns: 2.125fr 1fr;

  img {
    max-width: 100%;
  }
}

.visually-hidden {
  border: 0;
  clip: rect(0 0 0 0);
  height: 1px;
  margin: -1px;
  overflow: hidden;
  padding: 0;
  position: absolute !important;
  width: 1px;
}